'use strict';

/**
 * Operations on /node/{id}
 */
module.exports = {
    
    /**
     * Returns nodes based on ID
     * parameters: id
     * produces: application/json, text/html
     */
    get: function getNodesById(req, res) {
        res.send(501);
    }
    
};
